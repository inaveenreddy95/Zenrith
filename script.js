document.addEventListener("DOMContentLoaded",()=>{const toggle=document.querySelector(".menu-toggle"),nav=document.querySelector(".nav");if(toggle&&nav){toggle.addEventListener("click",()=>nav.classList.toggle("open"));nav.querySelectorAll("a").forEach(a=>a.addEventListener("click",()=>nav.classList.remove("open")))}});
function submitForm(e){e.preventDefault();const f=e.target;const name=f.name.value.trim(),email=f.email.value.trim(),service=f.service.value,message=f.message.value.trim();const subject=encodeURIComponent("Zenrith enquiry — "+service);const body=encodeURIComponent(`Name: ${name}\nWork Email: ${email}\nService: ${service}\n\nMessage:\n${message}`);window.location.href=`mailto:info@zenrith.com?subject=${subject}&body=${body}`;const note=document.getElementById("form-note");if(note)note.textContent="Your email client should open with the enquiry details. Please send the email to complete the enquiry.";return false;}